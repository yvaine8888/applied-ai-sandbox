# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: q is directly written into the sql statement so attackers can do SQL injection.
- Why Claude wrote it: Probably didn't think about it since no parameters or constraints.
- Fix: Use bound parameters

## Bug hunt #2 — hallucinated package
- Bug: flask_simpleauth does not exist
- Why Claude wrote it: Claude thought it existed. Maybe because of what data it was trained on.
- Fix: Tell claude the file does not exist with the function or create file.

## Bug hunt #3 — edge case
- Bug: When list is empty, it divides by zero, creating an error.
- Why Claude wrote it: Did not account for the error.
- Fix: Change so it accounts like when list is empty, return 0.

## Bug hunt #4 — hardcoded secret
- Bug: API is out in the open. Should be in .env or something else to protect secret.
- Why Claude wrote it: Did not account for privacy and directly used it in the file.
- Fix: Take it out and use .env file with git ignoring it.

## Test I wrote
- Bug I picked: Zero division
- Test code: Look at tests.
- Confirmed it fails on the bug and passes on the fix? yes

## Near-miss reflection
- I did not merge it, but I remember the bad context test. 
Claude assumed many things, which it did tell you, but sometimes you had to look closer. Another one was the good context. I thought it did not miss any line from the context, but if I had a good verification habit, maybe I would have found an error.