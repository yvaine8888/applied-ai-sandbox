# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — Add tags feature**: Change html to allow user to add tags but no python yet. CSS should style the tags. Verify: In the app, user can add tags with it styled.
2. **Task 2 — Connect to Python**: When users add tags, change python to store tags in a dictionary. Verify: Use print to verify dict was changed.
3. **Task 3 — Filter feature**: Change html and css to allow user to enter a tag to filter at the top. Verify: In the app, the filter system appears styled.
4. **Task 4 — Filter System**: Get the user input from filter system and filter through the dict list to find notes with it. Store it in a list. Python should be changed. Verify: Use print to verify list had added components.
5. **Task 5 — Display notes**: Show tags as chips next to note title in home page. Html should be changed and css to style. Verify: In the home page, all notes with tags should have it.
6. **Task 6 — Displaying result**: Display notes from filter list. Verify: When filtering, notes with associated tag is displayed.

## Task 1 context brief
Goal: **Task 1 — Add tags feature**: Change html to allow user to add tags but no python yet. CSS should style the tags. 
Constraints: New_note.html should be changed to allow user input strings or tags. CSS should style it.

## Task 1 — what happened
- Claude's plan summary: Done. templates/new_note.html:36-40 adds the comma-separated Tags input between Title and Body, with a hint line.
- What I pushed back on: Did not need to push back.
- Verification result: Worked when I ran the browser and passed all task 1 tests.

## Task 2 context brief
Goal: **Task 2 — Connect to Python**: When users add tags, change python to store tags in a dictionary with each note having a list of tags. Verify: Use print to verify dict was changed.
Constraints: Python should be changed and a test added to make sure the tags were added to dictionary.

## Task 2 — what happened
- Claude's plan summary: app.py — Read tags, append, and confirm dict shape for verification.
- What I pushed back on: Did not need to push back but had multiple errors because of the verification step and connecting.
- Verification result: All tests passed.

## Task 3 context brief
Goal: Goal: **Task 3 — Filter feature**: Change html and css to allow user to enter a tag to filter at the top. 
Constraints: home.html should be changed to allow user input tag. CSS should style it.

## Task 3 — what happened
- Claude's plan summary:  Filter form between the "+ New note" button and the notes heading, with styling. Leave a filter_tag Jinja placeholder so the input echoes the user's value. Matching the existing palette. No Python changes
- What I pushed back on: Did not need to push back but did assume some things.
- Verification result: Worked when I ran the browser and passed all task 1 tests.

## Reflection — where I redirected
- I didn't really have to redirect claude but there was many times it had to ask me for an option. This may have been because my context was too vague. There was one time in task 2 because it overcomplicated trying to verify.